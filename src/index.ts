// src/index.ts

export { EmailScribe } from './App';
export type {
  EmailScribeConfigProps as EmailScribeProps,
  EmailScribeUIProps,
} from './App';

// You might also want to export other useful components or utilities
export { useTheme } from './hooks/useTheme';
// Add any other exports that might be useful for library users
